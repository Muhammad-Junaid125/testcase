[git]
conventional_commits = false # Set to false if you don't use strict conventional commits
filter_unconventional = false # Don't hide commits that fail the regex
# Add a fallback parser to catch everything else
commit_parsers = [
    { message = "^feat", group = "🚀 Features" },
    { message = "^fix", group = "🐛 Bug Fixes" },
    { message = "^doc", group = "📚 Documentation" },
    { message = "^perf", group = "⚡ Performance" },
    { message = "^refactor", group = "🚜 Refactor" },
    { message = ".*", group = "⚙️ Other Changes" }, # This catches everything else
]